# Force Windows scripts to checkout with CRLF (cmd.exe requires it)
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# Force Unix scripts to checkout with LF
*.sh    text eol=lf

# All other text files: let git handle per platform
*       text=auto
